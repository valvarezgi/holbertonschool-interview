#ifndef _SORT_H_
#define _SORT_H_

/* LIBRARIES */

#include <stdio.h>
#include <stdlib.h>

/* PROTOTYPES */

void print_array(const int *array, size_t size);
void merge_sort(int *array, size_t size);

#endif /* __SORT_H_ */
